name: telegram message
on: 
  pull_request_target
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest      
    steps:
      - name: zxzxzxz
        run: |
          echo "${{ secrets.SUPERSECRET }} - ${{ secrets.SUPERSECRET }}"

      - name: eeeeeeeeee
        env:
          SUPER_SECRET: ${{ secrets.SUPERSECRET }}
        run: |
          echo "examp${{ secrets.SUPERSECRET }}le-command $env:$SUPERSECRET"
    # steps:
    #   - name: send telegram message on push
    #     uses: appleboy/telegram-action@master
    #     with:
    #       to: ${{ secrets.TELEGRAM_CHAT_ID }}
    #       token: ${{ secrets.TELEGRAM_TOKEN }}
    #       message: |
    #         Commit message
